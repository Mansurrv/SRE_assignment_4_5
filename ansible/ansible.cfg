[defaults]
inventory = inventories/example/hosts.ini
host_key_checking = False
retry_files_enabled = False
# Ansible 2.13+ removed community.general.yaml callback; use builtin default with YAML formatting.
stdout_callback = ansible.builtin.default
result_format = yaml

[privilege_escalation]
become = True
become_method = sudo
